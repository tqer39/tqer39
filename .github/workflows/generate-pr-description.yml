---
name: Generate PR Title and Description

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  generate-pr-description:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read # uses: actions/checkout
      pull-requests: write # uses: tqer39/openai-generate-pr-description
    # Check if the PR is not created by 'renovate' or 'tqer39-apps'
    if: contains(fromJSON('["renovate[bot]", "tqer39-apps[bot]"]'), github.event.pull_request.user.login) == false
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
      - uses: tqer39/openai-generate-pr-description@90dbc0ef9ea813159242481eaa1f63d97263f1c1 # v2.0.1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          api-key: ${{ secrets.OPENROUTER_API_KEY }}
          model: "openai/gpt-4o-mini"
          locale: "ja"
